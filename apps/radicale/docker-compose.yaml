services:
  radicale:
    image: "tomsquest/docker-radicale:3.4.1.0\
      @sha256:8b50b8c664d9557e47f723e18064d8f5f822e39d4e8a3d2385947ee13a2f8222"
    networks:
      caddy:
        aliases: ["${CADDY_NETWORK_RADICALE_ALIAS}"]
    volumes:
      - ./volumes/data:/data
    user: ${PUID}:${PGID}
    restart: ${DOCKER_RESTART}

networks:
  caddy:
    external: true
    name: ${COMPOSE_PROJECT_NAME}_${CADDY_NETWORK}
