- Investigate why dnsmasq sometimes uses 127.0.0.11 (Docker's default DNS) as upstream nameserver instead of the correct VPN-tunneled DNS. Removing the anonymous volume at `/etc/dnsmasq` and recreating the container fixes it temporarily, but root cause unknown. Temporary fix: `docker-compose rm -vf wireguard-gluetun-internal-dnsmasq && docker volume rm <volume-id> && docker-compose up -d`.
